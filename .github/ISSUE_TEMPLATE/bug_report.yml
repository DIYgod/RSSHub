name: |
  🐛 Bug report
  🐛 Bug 报告
description: |
  Submit discovered bugs
  早起的小可爱有虫抓
labels: ['RSS bug']

body:
  - type: markdown
    attributes:
      value: |
        Please ensure you have read [documentation](https://docs.rsshub.app/en), and provide all the information required by this template, otherwise the issue will be closed immediately.
        Due to the anti-crawling policy implemented by certain websites, some RSS routes provided by the demo will return status code 403. This is not an issue caused by RSSHub and please do not report it.

        请确保已阅读 [文档](https://docs.rsshub.app) 内相关部分，并按照模版提供信息，否则 issue 将被立即关闭。
        由于部分源网站反爬缘故，演示地址一些 RSS 会返回 status code 403，该问题不是 RSSHub 所致，请勿提交 issue。

  - type: input
    id: routes
    attributes:
      label: |
        Routes
        路由地址
      description: |
        The involved route, without any parameters
        涉及到的路由，不带任何参数
      placeholder: /rsshub/routes
    validations:
      required: true

  - type: input
    id: fullroutes
    attributes:
      label: |
       Full routes
       完整路由地址
      description: |
       The involved route, with all required and optional parameters
       涉及到的路由，带所有必须和可选参数
      placeholder: /rsshub/routes
    validations:
      required: true

  - type: input
    id: docs-link
    attributes:
      label: |
        Related documentation
        相关文档
      description: |
        Link to related documentation
        相关文档地址
      placeholder: https://docs.rsshub.app/...
    validations:
      required: true

  - type: textarea
    id: what-expected
    attributes:
      label: |
        What is expected?
        预期结果
    validations:
      required: true

  - type: textarea
    id: actual-happened
    attributes:
      label: |
        What is actually happening?
        实际发生了什么？
    validations:
      required: true

  - type: dropdown
    id: deployment
    attributes:
      label: |
        Deployment information
        部署
      multiple: false
      options:
        - RSSHub demo (https://rsshub.app)
        - |
          Self-hosted
          自建
    validations:
      required: true

  - type: input
    id: deploy-info
    attributes:
      label: |
        Deployment information (for self-hosted)
        自建部署相关信息
      description: |
        Please provide your OS, node version and docker version(if applicable)

        请提供您的操作系统、node 版本和（如果适用） docker 版本。
        请确保您部署的是[主线 master 分支](https://github.com/DIYgod/RSSHub/tree/master)最新版 RSSHub。
      placeholder: 'OS: Linux, Node: v10.15.3, Docker: v19.03.13'

  - type: textarea
    id: logs
    attributes:
      label: |
        Additional info
        额外信息
      description: |
        logs, errors, etc.
        日志、报错等
      render: shell
    validations:
      required: true

  - type: checkboxes
    id: terms
    attributes:
      label: |
        This is not a duplicated issue
        这不是重复的 issue
      options:
        - label: |
            I have searched [existing issues](https://github.com/DIYgod/RSSHub/issues) to ensure this bug has not already been reported
            我已经搜索了[现有 issue](https://github.com/DIYgod/RSSHub/issues)，以确保该错误尚未被报告。
          required: true
