{{if subtitle}}
  <h3>{{subtitle}}</h3>
{{/if}}

{{if pics.length}}
    {{each pics}}
        <div>
            <img src={{$value.url}} />
            {{if $value.picText }}
            <p>{{$value.picText}}</p>
            {{/if }}
        </div>
    {{/each}}
{{/if}}

{{@content}}
