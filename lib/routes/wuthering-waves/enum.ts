/** Route parameters. */
export enum Parameters {
    Limit = 'limit',
    Language = 'language',
    Category = 'category',
}

/** The article category (type). */
export enum ArticleType {
    Notice = 58,
    News = 57,
    Event = 59,
}
