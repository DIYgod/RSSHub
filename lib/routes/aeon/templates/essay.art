{{ if banner.url }}
<figure>
    <img src="{{ banner.url }}" alt="{{ banner.alt }}">
    {{ if banner.caption }}
    <figcaption>{{ banner.caption }}</figcaption>
    {{ /if }}
{{ /if }}
{{@ authorsBio }}
{{@ content }}
