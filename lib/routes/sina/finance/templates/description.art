{{ if rich_text_html }}
{{@ rich_text_html }}
{{ else if rich_text }}
{{@ rich_text }}
{{ /if }}
{{ if images }}
{{ each images image }}
<img src="{{ image }}" referrerpolicy="no-referrer" />
{{ /each }}
{{ /if }}
