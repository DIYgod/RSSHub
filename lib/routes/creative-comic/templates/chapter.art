{{ if chapter }}
    <p>{{ chapter.name }}</p>
{{ /if }}
{{ if pages }}
    {{ each pages page }}
        <br><img src="{{ page }}">
    {{ /each }}
{{ /if }}
{{ if cover }}
    <img src="{{ cover }}">
{{ /if }}
