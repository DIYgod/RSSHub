import type { Namespace } from '@/types';

export const namespace: Namespace = {
    name: 'Coomer',
    url: 'coomer.st',
    description: `::: tip
The route uses \`https://coomer.st\` by default. Self-hosted instances can override it with the \`COOMER_ROOT_URL\` environment variable.
:::`,
    lang: 'en',
};
