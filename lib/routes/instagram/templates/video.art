{{ if summary }}
    {{@ summary.replace(/\n/g, '<br>') }}
    <br>
{{ /if }}

<video controls preload="metadata" poster="{{ image }}" width="{{ video.width }}">
    <source src="{{ video.url }}" type="video/mp4">
</video>
