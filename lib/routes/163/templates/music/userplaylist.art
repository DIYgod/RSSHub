{{ if image }}
<img src="{{ image }}">
{{ /if }}

{{ if description }}
<div>
    {{ each description d }}
    <p>{{ d }}</p>
    {{ /each }}
</div>
{{ /if }}


{{ if src }}
<div><a href="{{ src }}">查看歌单</a></div>
{{ /if }}
