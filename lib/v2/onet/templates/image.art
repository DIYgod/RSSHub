<figure>
    <img src="{{ url }}" alt="{{ alt }}">
    <figcation>
        {{if caption != null }}
            <cite>{{ caption }}</cite> -
        {{/if}}
        {{ author }}
    </figcation>
</figure>
